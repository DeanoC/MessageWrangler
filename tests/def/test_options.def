namespace Test {
    message OptionsTest {
        /// Single option field
        singleOption: options { OptionA, OptionB, OptionC } = OptionA;

        /// Combined options field
        combinedOptions: options { OptionX, OptionY, OptionZ } = OptionX & OptionZ;

        /// Optional options field
        optional optionalOptions: options { Option1, Option2, Option3 };
    }
}