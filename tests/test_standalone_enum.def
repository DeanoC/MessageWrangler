enum TestEnum {
    Zero = 0,
    One = 1,
    Two = 2
}

open_enum TestOpenEnum {
    Zero = 0,
    One = 1,
    Two = 2
}

enum TestEnumWithInheritance : TestEnum {
    Three = 3,
    Four = 4
}

namespace TestNamespace {
    enum NamespacedEnum {
        Zero = 0,
        One = 1,
        Two = 2
    }
}

message TestMessage {
    field enumField: enum { Zero = 0, One = 1, Two = 2 }
}